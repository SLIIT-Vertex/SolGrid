# Copy this file to env.properties (gitignored, never committed) and set a real key.
# Alternatively, set MAPS_API_KEY in your user-level gradle.properties instead:
#   Windows: %USERPROFILE%\.gradle\gradle.properties
#   macOS/Linux: ~/.gradle/gradle.properties
MAPS_API_KEY=
